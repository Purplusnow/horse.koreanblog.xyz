# 버전을 고정한다.
#
# 모델은 joblib 피클로 릴리스에 보관했다가 다음 실행에서 되읽는다. 절인 쪽과
# 푸는 쪽의 scikit-learn 이 다르면 사설 클래스가 사라져 그대로 깨진다 —
# 실제로 로컬 1.6.1 로 만든 모델을 CI 가 받은 1.9.0 이 못 읽어 배포가 멈췄다
# (AttributeError: _RemainderColsList). pandas 3.0 처럼 동작이 바뀌는 상위 버전이
# 조용히 끼어드는 것도 같은 이유로 막는다.
#
# 올릴 때는 로컬에서 먼저 올려 학습·테스트를 통과시킨 뒤 이 파일을 함께 바꾼다.
requests==2.32.5
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
joblib==1.5.3
Jinja2==3.1.6
PyYAML==6.0.3
python-dotenv==1.0.1
anthropic==0.120.2
